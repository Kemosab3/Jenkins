package org.example;// Import the DistanceCalculatorApplication class if it's in a different package
import org.example.DistanceCalculatorApplication;

public class Main {
    public static void main(String[] args) {
        // Create an instance of DistanceCalculatorApplication
        DistanceCalculatorApplication app = new DistanceCalculatorApplication();

        // Call the run method to execute the distance calculation
        app.run();
    }
}
